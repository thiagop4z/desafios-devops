apiVersion: v1
kind: Pod
metadata:
  name: "{{ include "idwall.fullname" . }}-test-connection"
  labels:
    {{- include "idwall.labels" . | nindent 4 }}
  annotations:
    "helm.sh/hook": test
spec:
  containers:
    - name: wget
      image: busybox
      command: ['wget']
      args: ['{{ include "idwall.fullname" . }}:{{ .Values.service.port }}']
  restartPolicy: Never

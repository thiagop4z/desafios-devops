apiVersion: apps/v1
kind: Deployment
metadata:
  name: idwall
  namespace: default
spec:
  replicas: 1
  selector:
    matchLabels:
      app: idwall
  template:
    metadata:
      labels:
        app: idwall
    spec:
      containers:
      - name: idwall
        image: idwall:1.0
---
apiVersion: v1
kind: Service
metadata:
  name: idwall
  namespace: default
spec:
  type: NodePort
  selector:
    app: idwall
  ports:
  - port: 3000
    targetPort: 3000
    nodePort: 31000
---
apiVersion: networking.k8s.io/v1
kind: Ingress
metadata:
  name: idwall
spec:
  rules:
  - host: idwall.cc
    http:
      paths:
      - path: /
        pathType: Prefix
        backend:
          service:
            name: idwall
            port:
              number: 31000
---
apiVersion: v1
kind: ConfigMap
metadata:
  name: idwall
data:
  process.env.NAME: thiago
  name: thiago
  env.NAME: thiago
